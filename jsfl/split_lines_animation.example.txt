# Скопируйте в split_lines_animation.txt рядом со скриптом SplitMultilineTextToMovieClips.jsfl
# Сдвиг первого ключа относительно второго, пиксели (координаты сцены)
offset_x=0
offset_y=16
# Кадров между первым и вторым ключом (длина classic tween)
tween_frames=20
# Ступень по времени: каждый следующий слой split_lines на +N кадров позже предыдущего
layer_stagger=3
